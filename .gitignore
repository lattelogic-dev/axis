# .gitignore for a React + Vite project

### Dependencies ###
# The node_modules directory contains all of your project's dependencies.
# It can be very large and should always be generated locally by running `npm install`.
/node_modules

### Build output ###
# The dist directory is the default output folder for the production build.
# This should be generated by your CI/CD pipeline or before deployment, not stored in git.
/dist

### Vite ###
# Vite's internal cache directory.
/.vite/

### Logs ###
# Log files generated by npm or other tools during development or debugging.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

### Environment Variables ###
# Environment variable files should not be committed to version control.
# They often contain sensitive information like API keys.
# Create a .env.example file with placeholder values and commit that instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

### IDEs and Editors ###
# Configuration files for popular code editors.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

### Operating System Files ###
# Files generated by macOS, Windows, and Linux that are not part of the project.
.DS_Store
Thumbs.db

